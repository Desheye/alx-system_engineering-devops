#!/bin/bash

# SSH into the server using the specified private key and user
# Single-character flags are used for SSH options

# Set SSH options:
# -i: Specifies the identity file (private key)
# -l: Specifies the login name (not allowed, so omitted)
# -o: Allows specifying additional options (not used in this script)
# -p: Specifies the port to connect to (not required in this task)
# -A: Enables authentication agent forwarding (not required in this task)
# -T: Disables pseudo-terminal allocation (not required in this task)
# -q: Quiet mode, disables printing of diagnostic messages

ssh -i ~/.ssh/school -o User=ubuntu -q ubuntu@18.207.234.17

